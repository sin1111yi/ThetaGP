<!--
  Auto-generated by scripts/gen_proto.py — DO NOT EDIT MANUALLY
  Source: protocol/protocol.toml
  Source sha256: a3f7e969f380f0846b4ac2de2c5351cec3245e8f527bae2af594ad0ef1ace388
-->

# CDC 响应字段表（派生自 `protocol/protocol.toml`）

`python3 scripts/gen_proto.py --target fields-md` 的输出：每个有响应负载的命令
一张表。字段名（JSON 键）、类型与顺序逐字取自 `protocol.toml` 的 `response`
数组 —— 类型写的是 TOML 里声明的名字（`u32` / `string` / …），不经翻译，
以便逐字对拍；顺序即响应写出字段的顺序。

**本文件是派生物，手改无效**：协议改动一律改 `protocol.toml` 再重跑生成器。

协议版本：`1.1`

## `sys.get_fw_version`

Read firmware version and build info

| 字段 | 类型 | 数据源或说明 |
|------|------|--------------|
| `board` | string | — |
| `version` | string | — |
| `build_date` | string | — |
| `build_time` | string | — |

## `sys.get_task_info`

Get task scheduler statistics by TID

| 字段 | 类型 | 数据源或说明 |
|------|------|--------------|
| `tid` | i32 | — |
| `name` | string | — |
| `sub` | string | — |
| `desiredUs` | u32 | — |
| `avgCycleUs` | u32 | — |
| `actualHz` | u32 | — |
| `maxExecUs` | u32 | — |
| `avgExecUs` | u32 | Moving-average execution time, microseconds |
| `totalExecUs` | u32 | Accumulated execution time since boot, microseconds |
| `avgDeltaUs` | u32 | Moving-average interval between executions, microseconds |
| `runCount` | u32 | Times the task function has run, reported only in a build with the test API (testsys.cpp:128) |
| `lateCount` | u32 | Times the task missed its deadline, reported only in a build with the test API (testsys.cpp:128) |

## `sys.get_usage`

Aggregate resource usage: CPU load, MCU flash, MCU RAM, external SPI flash

| 字段 | 类型 | 数据源或说明 |
|------|------|--------------|
| `cpu_load_percent` | u32 | — |
| `task_count` | u32 | — |
| `mcu_flash_used_bytes` | u32 | — |
| `mcu_flash_total_bytes` | u32 | — |
| `ram_used_bytes` | u32 | — |
| `ram_total_bytes` | u32 | — |
| `ram_reserved_bytes` | u32 | — |
| `ram_dtcm_used_bytes` | u32 | — |
| `ram_axi_used_bytes` | u32 | — |
| `ram_d2_used_bytes` | u32 | — |
| `ram_d3_used_bytes` | u32 | — |
| `ram_itcm_used_bytes` | u32 | — |
| `ext_flash_total_sectors` | u32 | — |
| `ext_flash_used_sectors` | u32 | — |
| `ext_flash_free_sectors` | u32 | — |
| `ext_flash_reserved_sectors` | u32 | — |
| `profile_count` | u32 | — |

## `config.get_device_info`

Get device board name and firmware version

| 字段 | 类型 | 数据源或说明 |
|------|------|--------------|
| `board` | string | — |
| `fw_version` | string | — |
| `serial` | string | Device serial number |

## `config.set_key`

Set a configuration key-value pair (reserved)

| 字段 | 类型 | 数据源或说明 |
|------|------|--------------|
| `key` | string | — |

## `config.get_key`

Get a configuration key value (reserved)

| 字段 | 类型 | 数据源或说明 |
|------|------|--------------|
| `key` | string | — |
| `value` | any | — |

## 无响应负载的命令

这些命令的 `response` 为空：响应只有通用字段（`status` / `cmd` / `queued`）：

- `sys.ping`
- `sys.reset`
- `sys.enter_dfu`
- `config.save`
- `config.load`
- `config.factory_reset`

